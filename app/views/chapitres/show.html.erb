<p id="notice"><%= notice %></p>

<p>
  <strong>Nom:</strong>
  <%= @chapitre.name %>
</p>

<p>
  <strong>Numero:</strong>
  <%= @chapitre.numero %>
</p>
<p>
  <strong>Anecdote correspondante:</strong>
  <% @chapitre.anecdotes.each do |c| %>

 <p> <small>sujet: </small><%= c.sujet %>
  <small>theme: </small><%= c.theme %></p>
  <% end %>

</p>

<p>
 <strong>Scene correspondante:</strong>
 <% @chapitre.scenes.each do |scene| %>
 <p><small>recit: </small><%= scene.recit %>
 <small>lieu: </small><%=scene.lieu %>
 <small>period: </small><%=scene.period %></p>
 <% end %>
</p>

<%= link_to 'Editer', edit_chapitre_path(@chapitre) %> |
<%= link_to 'Retour', chapitres_path %>
