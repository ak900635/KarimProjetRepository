<h1>Listing scenes</h1>

<table>
  <thead>
    <tr>
      <th>Period</th>
      <th>Recit</th>
      <th>Lieu</th>
      <th>IDChapitre</th>
      <th> N°Scene </th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @scenes.each do |scene| %>
      <tr>
        <td><%= scene.period %></td>
        <td><%= scene.recit %></td>
        <td><%= scene.lieu %></td>
        <td><%= scene.chapitre_id %></td>
        <td><%= scene.id %></td>
        <td><%= link_to 'Details', scene %></td>
        <td><%= link_to 'Editer', edit_scene_path(scene) %></td>
        <td><%= link_to 'Detruire', scene, method: :delete, data: { confirm: 'Etes vous sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Creer Nouvelle Scene', new_scene_path %>
